# ====== دوّنلي — متغيّرات البيئة ======

# توكن بوت تليجرام (من @BotFather)
TELEGRAM_BOT_TOKEN=
# اسم البوت من غير @ — لزرار "اربط تيليجرام" في الداشبورد
TELEGRAM_BOT_USERNAME=dawenli_bot

# مفتاح OpenAI
OPENAI_API_KEY=

# الـ chat_id بتاع صاحب المنصة (الأدمن)
# لو سِبته فاضي، أول مرة تكلّم البوت هيقولك الـ chat_id بتاعك
ALLOWED_CHAT_ID=

# افتح التسجيل لأي حد يكلم البوت (منصة multi-user)
# لو false: البوت يرد على ALLOWED_CHAT_ID بس (الوضع الشخصي)
OPEN_SIGNUP=false

# موديلات OpenAI
# gpt-4o-transcribe: أدق بكتير في العربي من whisper-1
OPENAI_TRANSCRIBE_MODEL=gpt-4o-transcribe
# موديل الـ agent (لازم يدعم tool calling كويس — لو عايز توفّر حط gpt-4o-mini)
OPENAI_AGENT_MODEL=gpt-4o
OPENAI_ANALYSIS_MODEL=gpt-4o
# ملحوظة: OPENAI_CHAT_MODEL القديم مبقاش مستخدم — امسحه من .env لو لسه موجود

# الداشبورد
PORT=3000
DASHBOARD_PASSWORD=change-me

# مسار قاعدة البيانات
DB_PATH=./data/dawenli.db

# مواعيد المبادرة (بتوقيت القاهرة)
TIMEZONE=Africa/Cairo
CHECKIN_HOUR=21
WEEKLY_DAY=5
WEEKLY_HOUR=20

# ====== إشعارات PWA (Web Push) — اختياري ======
# لو سِبتهم فاضيين، التطبيق بيتثبّت عادي بس زرار التنبيهات بيتخفي.
# ولّد مفاتيح جديدة بـ: node -e "console.log(require('web-push').generateVAPIDKeys())"
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:you@example.com
